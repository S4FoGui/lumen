# ============================================================
# Lumen — Configuração Padrão (v1.0.2 — KDE/Debian fix)
# ============================================================

# --- Áudio ---
audio:
  device: null
  sample_rate: 16000
  channels: 1

# --- Transcrição ---
transcription:
  model_path: null
  language: "pt"
  lightning_mode: false
  # ✅ FIX: Reduzido de 4000ms para 1200ms — mais responsivo
  auto_send: false
  silence_threshold_ms: 1200
  voice_commands_enabled: true
  always_listening: false
  wake_word: "lumen"
  filler_words:
    - "humm"
    - "ééé"
    - "ãhh"
    - "uhh"
    - "hmm"
    - "uhm"
    - "eh"
    - "ah"
    - "tipo assim"
    - "né"
    - "então"
    - "bom"
    - "um"
    - "uh"
    - "you know"
    - "like"
    - "actually"
    - "basically"
    - "literally"

# --- Atalhos ---
hotkeys:
  toggle_recording: "enter"
  lightning_mode: "ctrl+shift+l"
  open_dashboard: "ctrl+shift+d"

# --- Injeção de Texto ---
text_injection:
  # ✅ FIX: Forçar clipboard no KDE/Plasma — xdotool não funciona bem no Wayland
  method: "clipboard"
  delay_ms: 150

# --- Inteligência Artificial ---
ai:
  provider: "disabled"
  auto_formatting: false
  ollama:
    url: "http://localhost:11434"
    api_key: ""
    model: "llama3.2"
  omniroute:
    url: "http://localhost:20128/v1"
    api_key: ""
    model: "cc/claude-3-5-sonnet"
  openai:
    api_key: ""
    model: "gpt-4o-mini"
  gemini:
    api_key: ""
    model: "gemini-2.0-flash"
  groq:
    api_key: ""
    model: "llama-3.3-70b-versatile"
  default_instruction: "You are an advanced voice dictation pre-processor. The input is raw speech-to-text. Your goal is to completely remove language fillers (e.g., um, ah, you know, tipo, né, então), clean up accidental repetitions, and fix stutters. Apply perfect grammar and punctuation while preserving 100% of the original meaning and natural tone. CRITICAL RULE: You MUST output the response in the EXACT same language as the input (e.g., if the user speaks in English, output in English; if Portuguese, output in Portuguese). Do NOT translate the text."

# --- Snippets ---
snippets:
  entries:
    "/ola": "Olá! Tudo bem? Como posso ajudar?"
    "/email": "Atenciosamente,\nGuilherme"
    "/obg": "Muito obrigado pela atenção!"

# --- Dicionário Customizado ---
dictionary:
  entries:
    "react":
      value: "React"
      context: "Web Development"
      icon_type: "brand"
    "javascript":
      value: "JavaScript"
      context: "Web Development"
      icon_type: "brand"
    "typescript":
      value: "TypeScript"
      context: "Web Development"
      icon_type: "brand"
    "python":
      value: "Python"
      context: "Software Development"
      icon_type: "code"
    "linux":
      value: "Linux"
      context: "Operating Systems"
      icon_type: "os"
    "github":
      value: "GitHub"
      context: "Version Control"
      icon_type: "git"

# --- Interface ---
ui:
  dashboard_port: 8484
  open_on_start: false
  show_overlay: true
  show_tray: true

# --- Logging ---
logging:
  level: "info"
  file: null
